#!/bin/sh

set -e

if [ "$1" = install ] && ! [ -L "$DPKG_ROOT/lib/#DEB_HOST_MULTIARCH#/libz.so.1" ]; then
    ln -s libz.so.#ZLIB_VERSION# "$DPKG_ROOT/lib/#DEB_HOST_MULTIARCH#/libz.so.1"
fi

#DEBHELPER#
